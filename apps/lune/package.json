{
  "name": "lune",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "client:dev": "cd ./client && yarn dev",
    "server:dev": "ts-node index.ts",
    "migrate": "ts-node ./scripts/migrations.ts",
    "server:build": "rimraf dist && tsc -p build.tsconfig.json",
    "client:build": "cd ./client && yarn build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@lunejs/admin-ui": "0.1.4",
    "@lunejs/core": "0.1.7",
    "@lunejs/email-plugin": "^0.1.0",
    "@lunejs/paypal-plugin": "^0.1.0",
    "@lunejs/s3": "0.1.1",
    "dotenv": "^17.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^25.0.3"
  }
}
